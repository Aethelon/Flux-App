#!/bin/sh

echo "pre-push: type-check"
npm run typecheck || { echo "pre-push: type-check failed — push aborted"; exit 1; }

echo "pre-push: lint"
npm run lint || { echo "pre-push: lint failed — push aborted"; exit 1; }
